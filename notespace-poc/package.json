{
  "name": "thirdrailify-notespace-poc",
  "version": "0.1.0",
  "private": true,
  "description": "Isolated local-only Third Railify Notespace prototype. No production deployment.",
  "scripts": {
    "start": "node server.cjs --open",
    "test": "node --test tests/*.test.cjs",
    "bundle": "node scripts/build-standalone.cjs"
  },
  "engines": {
    "node": ">=22"
  }
}
