# Optional: use the Connections button in the running app instead.
# .env is the canonical local configuration; existing nonempty keys are preserved.
# Do not put keys in public/, source files or VITE_ variables.
REPLICATE_API_TOKEN=
OPENAI_API_KEY=
XAI_API_KEY=
OPENAI_IMAGE_MODEL=gpt-image-2.5-sunburst
OPENAI_CHAT_MODEL=gpt-6-astra
XAI_IMAGE_MODEL=grok-imagine-image-2.0
XAI_CHAT_MODEL=grok-4.6
# Retrieved from Replicate, NOT a secret you invent. Unused by the local polling POC.
REPLICATE_WEBHOOK_SIGNING_SECRET=
PORT=4317
